[build-system]
requires = [
    "pydub",
    "pygame",
    "requests",
    "pyautogui",
    "inputs",
    "pillow",
    "wxPython",
    "randstr",
    "setuptools >=42"
]
build-backend = "setuptools.build_meta"
